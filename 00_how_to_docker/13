#13. Print the environment variables of the spawning-pool container in one command,
#to be sure that you have configured your container properly

docker container inspect --format='{{ .Config.Env }}' spawning-pool
