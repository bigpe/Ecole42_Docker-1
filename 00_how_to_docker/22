#22. Turn Aiur into a slave of the local swarm in which Char is leader (the command to
#take control of Aiur is not requested).

TOKEN=`docker-machine ssh Char docker swarm join-token worker -q`
IP=`docker-machine ip Char`
docker-machine ssh Aiur docker swarm join --token $TOKEN $IP
